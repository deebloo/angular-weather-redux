########################
# Customize the test machine
########################
machine:
  environment:
    NODE_ENV: ci
  node:
    version:
      5.7.0

########################
# Customize test commands
########################
test:
  override:
    - npm test
